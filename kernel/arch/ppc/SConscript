# -*- Mode: Python -*-

Import('kernel_env')

kernel_env.Program(target = 'ppc.lo', source = [
Glob('*.[Sc]'),
kernel_env['BSP']+'/' + kernel_env['BSP'] + '.lo',
'libfdt/libfdt.lo'])

SConscript(kernel_env['BSP']+'/SConscript')
SConscript('libfdt/SConscript')

# EOF
