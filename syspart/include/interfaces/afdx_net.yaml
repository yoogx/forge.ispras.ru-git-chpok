- name: afdx_queue_enqueuer
  #afdx_queue_adder
  additional_h_files: ['<ret_type.h>']
  functions:
      - name: afdx_add_to_queue
        return_type: ret_t
        # component, packet, packet size, max_backstep, frontstep
        args_type: [self_t *, char *, size_t, size_t, size_t]
