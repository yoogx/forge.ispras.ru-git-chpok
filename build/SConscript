# -*- Mode: Python -*-

Import('env')

SConscript('../kernel/SConscript', variant_dir = env['ARCH']+'/kernel', duplicate = 0)
SConscript('../libpok/SConscript', variant_dir = env['ARCH']+'/libpok', duplicate = 0)

Export('env')

# EOF
