# -*- Mode: Python -*-

Import('env')

SConscript('../boards/SConscript', variant_dir = env['BSP']+'/boards', duplicate = 0)
SConscript('../kernel/SConscript', variant_dir = env['BSP']+'/kernel', duplicate = 0)
SConscript('../libpok/SConscript', variant_dir = env['BSP']+'/libpok', duplicate = 0)
SConscript('../syspart/SConscript',variant_dir = env['BSP']+'/syspart', duplicate = 0)

Export('env')

# EOF
