Тестовый пример для программного интерфейса дерева конфигурационных параметров (proptree).

Прмер создаёт простое дерево и читает строковое значение из одного из узлов.

В тесте используется следующее дерево:
a
	b
		c1 = (int)100
		c2 = (int)200
		c3 = "Hello, world!"
		
В примере читается и выводится на экран значение узла c3.
